import { libraryConfig } from "@read-master/config/eslint";

/**
 * ESLint configuration for packages/shared (shared types and utils)
 */

/** @type {import('eslint').Linter.Config[]} */
export default [...libraryConfig];
