import { describe, it, expect } from "vitest";
import { VoteButtons } from "./VoteButtons";

describe("VoteButtons", () => {
  it("should export the component", () => {
    expect(VoteButtons).toBeDefined();
  });
});
