import { describe, it, expect } from "vitest";
import { CreateEditListDialog } from "./CreateEditListDialog";

describe("CreateEditListDialog", () => {
  it("should export the component", () => {
    expect(CreateEditListDialog).toBeDefined();
  });
});
