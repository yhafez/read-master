import { describe, it, expect } from "vitest";
import { ShareCurriculumDialog } from "./ShareCurriculumDialog";

describe("ShareCurriculumDialog", () => {
  it("should export the component", () => {
    expect(ShareCurriculumDialog).toBeDefined();
  });
});
