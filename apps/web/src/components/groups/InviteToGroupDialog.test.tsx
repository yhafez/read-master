import { describe, it, expect } from "vitest";
import { InviteToGroupDialog } from "./InviteToGroupDialog";

describe("InviteToGroupDialog", () => {
  it("should export the component", () => {
    expect(InviteToGroupDialog).toBeDefined();
  });
});
