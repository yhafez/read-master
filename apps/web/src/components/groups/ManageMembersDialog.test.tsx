import { describe, it, expect } from "vitest";
import { ManageMembersDialog } from "./ManageMembersDialog";

describe("ManageMembersDialog", () => {
  it("should export the component", () => {
    expect(ManageMembersDialog).toBeDefined();
  });
});
