import { describe, it, expect } from "vitest";
import { GroupReadingSchedule } from "./GroupReadingSchedule";

describe("GroupReadingSchedule", () => {
  it("should export the component", () => {
    expect(GroupReadingSchedule).toBeDefined();
  });
});
