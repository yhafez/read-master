import { describe, it, expect } from "vitest";
import { ShareHighlightDialog } from "./ShareHighlightDialog";

describe("ShareHighlightDialog", () => {
  it("should export the component", () => {
    expect(ShareHighlightDialog).toBeDefined();
  });
});
