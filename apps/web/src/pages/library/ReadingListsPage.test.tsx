import { describe, it, expect } from "vitest";
import { ReadingListsPage } from "./ReadingListsPage";

describe("ReadingListsPage", () => {
  it("should export the component", () => {
    expect(ReadingListsPage).toBeDefined();
  });
});
