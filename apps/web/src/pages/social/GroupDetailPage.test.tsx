import { describe, it, expect } from "vitest";
import { GroupDetailPage } from "./GroupDetailPage";

describe("GroupDetailPage", () => {
  it("should export the component", () => {
    expect(GroupDetailPage).toBeDefined();
  });
});
