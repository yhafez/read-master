import { describe, it, expect } from "vitest";
import { CreateEditGroupPage } from "./CreateEditGroupPage";

describe("CreateEditGroupPage", () => {
  it("should export the component", () => {
    expect(CreateEditGroupPage).toBeDefined();
  });
});
