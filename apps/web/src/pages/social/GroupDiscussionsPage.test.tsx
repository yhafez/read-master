import { describe, it, expect } from "vitest";
import { GroupDiscussionsPage } from "./GroupDiscussionsPage";

describe("GroupDiscussionsPage", () => {
  it("should export the component", () => {
    expect(GroupDiscussionsPage).toBeDefined();
  });
});
