import { describe, it, expect } from "vitest";
import { ForumCategoryPage } from "./ForumCategoryPage";

describe("ForumCategoryPage", () => {
  it("should export the component", () => {
    expect(ForumCategoryPage).toBeDefined();
  });
});
