import { describe, it, expect } from "vitest";
import { ForumHomePage } from "./ForumHomePage";

describe("ForumHomePage", () => {
  it("should export the component", () => {
    expect(ForumHomePage).toBeDefined();
  });
});
