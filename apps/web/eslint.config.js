import { reactConfig } from "@read-master/config/eslint";

/**
 * ESLint configuration for apps/web (React frontend)
 */

/** @type {import('eslint').Linter.Config[]} */
export default [...reactConfig];
