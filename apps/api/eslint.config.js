import { nodeConfig } from "@read-master/config/eslint";

/**
 * ESLint configuration for apps/api (Vercel serverless)
 */

/** @type {import('eslint').Linter.Config[]} */
export default [...nodeConfig];
